# Dependencies
node_modules/

# Build artifacts
dist/
out/
build/
coverage/

# Environment
.env
.env.local

# IDE
.vscode/
.idea/
*.swp
*.swo

# OS
Thumbs.db
.DS_Store

# Data (SQLite) - Keep local dev data out of git, but we might want schema to be in. 
# The prompt says 'data/app.db', so we should ignore the actual DB file but maybe keep the folder gitkeep if empty.
data/*.db
data/*.db-journal
data/*.db-wal

# Logs
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
bun.lockb
